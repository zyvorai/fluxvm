# Copyright 2026 Zyvor AI Labs · https://zyvor.dev
# SPDX-License-Identifier: Apache-2.0

[package]
name = "fluxvm-guest-protocol"
version = "0.3.0"
edition = "2024"
license = "Apache-2.0"
description = "Wire protocol shared by fluxvm-guest-agent (in-guest) and fluxvm-vsock-client (host)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
