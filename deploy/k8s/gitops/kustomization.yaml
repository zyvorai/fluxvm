apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: fluxvm
resources:
  - ../namespace.yaml
  - ../rbac.yaml
  - ../configmap.yaml
  - ../crd.yaml
  - ../daemonset.yaml
commonLabels:
  app.kubernetes.io/part-of: zyvor
  zyvor.dev/component: fluxvm
